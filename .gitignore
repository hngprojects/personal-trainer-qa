# Dependencies
node_modules/

# Environment / secrets
.env
.env.local

# Newman / test output (keep curated reports, ignore raw run artifacts)
newman/
reports/latest-newman-report.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
CREDS.txt

# Real credentials — never commit
postman/CREDS.txt
# Env holds real staging creds at runtime (new tsx harness reads it directly) — never commit
postman/staging.postman_environment.json
# Raw per-run newman artifacts
reports/*-newman-report.json